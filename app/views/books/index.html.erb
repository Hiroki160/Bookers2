<%= render 'list', book: @book %>

<h1>Books</h1>
<table>
  <thead>
    <th>Title</th>
    <th>Opinion</th>
  </thead>

<div>
  <% @books.each do |book| %>
  <p><%= link_to book.title, book_path(book.id) %></p>
  <p><%= book.body %></p>
</div>
<% end %>